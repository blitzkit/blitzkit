---
import { api } from "../../core/api/dynamic";

const tanksList = await api.tanks();
---

<h1>tanks</h1>

{
  tanksList.tanks.map(({ id, slug }) => (
    <p>
      <a href={`/tanks/${slug}`}>
        {slug} ({id})
      </a>
    </p>
  ))
}
