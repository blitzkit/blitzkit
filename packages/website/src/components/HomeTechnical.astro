---
import { Heading } from "@radix-ui/themes";
import { tools } from "../constants/tools";
import { getStrings } from "../core/i18n/getStrings";
import ToolCard from "./ToolCard.astro";
import ToolCardLayoutColumn from "./ToolCardLayoutColumn.astro";
import ToolCardLayoutRoot from "./ToolCardLayoutRoot.astro";

interface Props {
  locale: string;
}

const { locale } = Astro.props;

const strings = getStrings(locale);
---

<Heading size="8">{strings.home.technical}</Heading>

<ToolCardLayoutRoot minor>
  <ToolCardLayoutColumn>
    <ToolCard {locale} tool={tools.api} />
  </ToolCardLayoutColumn>

  <ToolCardLayoutColumn>
    <ToolCard {locale} tool={tools.changelogs} />
  </ToolCardLayoutColumn>
</ToolCardLayoutRoot>
