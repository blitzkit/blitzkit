---
import { api } from "../api/dynamic";
import { tools } from "../constants/tools";
import { Heading } from "./Heading";
import { Section } from "./Section";
import ToolCard from "./ToolCard.astro";
import ToolCardLayoutColumn from "./ToolCardLayoutColumn.astro";
import ToolCardLayoutRoot from "./ToolCardLayoutRoot.astro";

interface Props {
  locale: string;
}

const { locale } = Astro.props;
const strings = await api.strings(locale);
---

<Section>
  <Heading size="2">{strings.home.tanks}</Heading>

  <ToolCardLayoutRoot>
    <ToolCardLayoutColumn major>
      <ToolCard {locale} tool={tools.tanks} />
    </ToolCardLayoutColumn>

    <ToolCardLayoutColumn>
      <ToolCard {locale} minor tool={tools.compare} />
      <ToolCard {locale} minor tool={tools.performance} />
    </ToolCardLayoutColumn>
  </ToolCardLayoutRoot>
</Section>
