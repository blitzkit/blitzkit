# BlitzKit 1.30.8

Support for tanks with Chinese names.

## Fixes

- The URL generator for BlitzKit now handles names that have only Chinese characters
- The Mixed Virtual File System now handles non-existent directories correctly
